# Enforce pnpm with hardened install defaults
package-manager=pnpm@9.15.5
frozen-lockfile=true
prefer-frozen-lockfile=true
minimum-release-age=3d
# Allow lifecycle scripts only for explicitly listed packages
only-built-dependencies[]=fsevents
